# Local API (backend PORT in backend/.env — default 3000)
# For remote access, replace with a live tunnel URL after running: cloudflared tunnel --url http://localhost:3000
VITE_API=http://localhost:3000
VITE_AUCTION_API=http://localhost:3000/auction
